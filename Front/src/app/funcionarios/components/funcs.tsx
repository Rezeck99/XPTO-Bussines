export type funcsProps = {
  name: string;
  cpf: string;
  dataNasc: string;
  dpt: string;
  entrada: string;
  saida: string;
  funcao: string;
  matricula: string;
};
function Funcs({ funcs }: { funcs: funcsProps[] }) {
  return <></>;
}

export default Funcs;
